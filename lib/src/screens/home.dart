import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("เริ่มต้นความสนุกกับ EZtrip"),
      ),
      body: Container(
        color: Colors.white,
      ),
    );
  }
}
